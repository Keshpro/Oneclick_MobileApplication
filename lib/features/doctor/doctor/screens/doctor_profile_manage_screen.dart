import 'package:flutter/material.dart';

class DoctorProfileManageScreen extends StatelessWidget {
  const DoctorProfileManageScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Manage doctor profile')));
}
